<div id="<%= dom_id category %>">
  <div class="cateName">
  <%= category.name %>
  </div>
  <%= link_to t('.edit'), edit_category_path(category), class:"btnGn btnEdit "%>
  <%= link_to t('.delete'), category_path(category), data: {
    turbo_method: :delete,
    turbo_confirm: t('common.confirm')
  }, class:"btnGn btnDelete " %>
</div>